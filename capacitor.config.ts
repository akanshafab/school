import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.adorta.madarskom',
  appName: 'Madarskom',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
